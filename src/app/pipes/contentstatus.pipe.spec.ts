import { ContentstatusPipe } from './contentstatus.pipe';

describe('ContentstatusPipe', () => {
  it('create an instance', () => {
    const pipe = new ContentstatusPipe();
    expect(pipe).toBeTruthy();
  });
});
